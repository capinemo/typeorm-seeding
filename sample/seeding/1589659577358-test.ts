import { Connection } from 'typeorm';
import { Seeder, Factory } from '../../src/types'

export class Seed1589659577358Test implements Seeder {
    public async run (factory: Factory, connection: Connection): Promise<any> {

    }
}
